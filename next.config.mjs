/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      serverActions: true,
    },
    images: {
      domains: ['aezgtfuplpokzckerqok.supabase.co'],
    },
  };
  
export default nextConfig;
